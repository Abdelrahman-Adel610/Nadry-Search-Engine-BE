import java.net.MalformedURLException;
import java.net.URL;

import com.google.code.findbugs.robots.BaseRobotsParser;
import com.google.code.findbugs.robots.RobotsMatcher;
import com.google.code.findbugs.robots.RobotsParser; // Concrete implementation

public class RobotChecker {
	private static String BOT_AGENT = "ndry/1.0";
	
	public static boolean isUrlAllowed(String urlToBeChecked) {
		// needed form  protocol + "://" + host + ":" + port;
		URL url;
		try {
			url = new URL(urlToBeChecked);
			String protocol = url.getProtocol();
			String host = url.getHost();
			int port = url.getPort();
			port = port == -1 ? url.getDefaultPort():port;
			String key = protocol + "://" + host + ":" + port;
			
			//	Calling RobotMatcher
			RobotsMatcher matcher = getRobotsMatcher(hostKey);
			
			
		} catch (MalformedURLException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}
}
